```
┌─────────────────────────────────────────────────────────────────────────────┐
│  tabela: iot_dispositivos_historico                                         │
├──────────────────────────┬──────────────────────┬───────────────────────────┤
│ coluna                   │ tipo                 │ constraints / notas       │
├──────────────────────────┼──────────────────────┼───────────────────────────┤
│ id                       │ UUID                 │ PK                        │
│ dispositivo_id           │ UUID                 │ NOT NULL                  │
│                          │                      │ FK → iot_dispositivos(id) │
│ ecoponto_id              │ UUID                 │ nullable                  │
│                          │                      │ FK → ecopontos(id)        │
│                          │                      │ NULL = desinstalado /     │
│                          │                      │ armazém                   │
│ estado_anterior          │ VARCHAR(20)          │ NOT NULL                  │
│ estado_novo              │ VARCHAR(20)          │ NOT NULL                  │
│ motivo                   │ TEXT                 │ nullable                  │
│                          │                      │ ex: "Avaria por vandalismo│
│                          │                      │  reportada em report #X"  │
│ alterado_por             │ UUID                 │ nullable                  │
│                          │                      │ FK → users(id)            │
│ alterado_em              │ TIMESTAMPTZ          │ NOT NULL, DEFAULT now()   │
└──────────────────────────┴──────────────────────┴───────────────────────────┘

Índices: (dispositivo_id, alterado_em DESC)
         (ecoponto_id, alterado_em DESC)
```