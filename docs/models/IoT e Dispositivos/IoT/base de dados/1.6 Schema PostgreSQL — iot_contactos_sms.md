```
┌─────────────────────────────────────────────────────────────────────────────┐
│  tabela: iot_contactos_sms                                                  │
├──────────────────────────┬──────────────────────┬───────────────────────────┤
│ coluna                   │ tipo                 │ constraints / notas       │
├──────────────────────────┼──────────────────────┼───────────────────────────┤
│ id                       │ UUID                 │ PK                        │
│ zona_id                  │ UUID                 │ NOT NULL                  │
│                          │                      │ FK → zonas(id)            │
│ nome_contacto            │ VARCHAR(150)         │ NOT NULL                  │
│ telefone                 │ VARCHAR(20)          │ NOT NULL                  │
│ papel                    │ VARCHAR(50)          │ nullable                  │
│                          │                      │ ex: 'Líder de Junta',     │
│                          │                      │ 'Associação de Moradores' │
│ tipos_alerta             │ VARCHAR(20)[]        │ NOT NULL                  │
│                          │                      │ DEFAULT                   │
│                          │                      │ '{ECOPONTO_CHEIO}'        │
│                          │                      │ CHECK each IN tipos       │
│                          │                      │ válidos                   │
│ ativo                    │ BOOLEAN              │ NOT NULL, DEFAULT true    │
│ opt_in_em                │ TIMESTAMPTZ          │ NOT NULL, DEFAULT now()   │
│                          │                      │ RF-27: opt-in explícito   │
│ opt_out_em               │ TIMESTAMPTZ          │ nullable                  │
│ criado_por               │ UUID                 │ nullable                  │
│                          │                      │ FK → users(id)            │
│ criado_em                │ TIMESTAMPTZ          │ NOT NULL, DEFAULT now()   │
└──────────────────────────┴──────────────────────┴───────────────────────────┘

Índices:
  (zona_id, ativo) PARTIAL WHERE ativo=true  → lista de contactos por zona
  (telefone)                                 → verificar duplicados
```