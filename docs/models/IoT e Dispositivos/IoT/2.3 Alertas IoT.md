| #   | Método  | Rota                              | Descrição                                                     | Auth                                      | Fluxo                                        |
| --- | ------- | --------------------------------- | ------------------------------------------------------------- | ----------------------------------------- | -------------------------------------------- |
| A1  | `GET`   | `/iot/alertas`                    | Listar alertas com filtros `?estado=&tipo=&zona_id=&de=&ate=` | OPERADOR_VEOLIA, TECNICO_AUTARQUIA, ADMIN | NestJS → PG réplica                          |
| A2  | `GET`   | `/iot/alertas/:id`                | Detalhe do alerta                                             | OPERADOR_VEOLIA, ADMIN                    | NestJS → PG réplica                          |
| A3  | `PATCH` | `/iot/alertas/:id/reconhecer`     | Operador reconhece o alerta                                   | OPERADOR_VEOLIA, ADMIN                    | NestJS → PG write reconhecido_por + em       |
| A4  | `PATCH` | `/iot/alertas/:id/resolver`       | Marcar como resolvido com nota                                | OPERADOR_VEOLIA, ADMIN                    | NestJS → PG write resolvido_em + notas       |
| A5  | `PATCH` | `/iot/alertas/:id/falso-positivo` | Marcar como falso positivo (calibração incorrecta)            | TECNICO_AUTARQUIA, ADMIN                  | NestJS → PG write → feedback para calibração |
| A6  | `GET`   | `/iot/alertas/kpis`               | TMEF, MTTR, alertas por tipo/zona `?zona_id=&periodo=`        | TECNICO_AUTARQUIA, ADMIN                  | FastAPI → Redis cache 15min ou PG agregação  |

**Corpo de A4 (resolver):**

```
{
  notas_resolucao: string (required),
  acao_tomada: "RECOLHA_EFECTUADA" | "SENSOR_REPARADO"
             | "CALIBRACAO_AJUSTADA" | "OUTRO" (optional)
}
```

---
