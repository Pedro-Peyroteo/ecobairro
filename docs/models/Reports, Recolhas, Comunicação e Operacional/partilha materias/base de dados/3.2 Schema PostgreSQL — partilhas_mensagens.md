Mensagens mediadas entre cidadãos interessados — sem expor contactos directamente (RF-15).

```
┌─────────────────────────────────────────────────────────────────────────────┐
│  tabela: partilhas_mensagens                                                │
├──────────────────────────┬──────────────────────┬───────────────────────────┤
│ coluna                   │ tipo                 │ constraints / notas       │
├──────────────────────────┼──────────────────────┼───────────────────────────┤
│ id                       │ UUID                 │ PK                        │
│ partilha_id              │ UUID                 │ NOT NULL                  │
│                          │                      │ FK → partilhas_materiais  │
│ remetente_id             │ UUID                 │ NOT NULL                  │
│                          │                      │ FK → users(id)         │
│ destinatario_id          │ UUID                 │ NOT NULL                  │
│                          │                      │ FK → users(id)         │
│ texto                    │ TEXT                 │ NOT NULL                  │
│                          │                      │ moderado antes de entrega │
│ lida                     │ BOOLEAN              │ NOT NULL, DEFAULT false   │
│ criado_em                │ TIMESTAMPTZ          │ NOT NULL, DEFAULT now()   │
└──────────────────────────┴──────────────────────┴───────────────────────────┘

Índices:
  (partilha_id, criado_em ASC)              BTREE — histórico da conversa
  (destinatario_id, lida, criado_em DESC)   BTREE PARTIAL WHERE lida=false
```