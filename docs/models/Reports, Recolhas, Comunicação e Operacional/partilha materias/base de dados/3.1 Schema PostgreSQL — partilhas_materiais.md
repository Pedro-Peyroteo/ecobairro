```
┌─────────────────────────────────────────────────────────────────────────────┐
│  tabela: partilhas_materiais                                                │
├──────────────────────────┬──────────────────────┬───────────────────────────┤
│ coluna                   │ tipo                 │ constraints / notas       │
├──────────────────────────┼──────────────────────┼───────────────────────────┤
│ id                       │ UUID                 │ PK, gen_random_uuid()     │
│ cidadao_id               │ UUID                 │ NOT NULL                  │
│                          │                      │ FK → users(id)            │
│ titulo                   │ VARCHAR(200)         │ NOT NULL                  │
│ descricao                │ TEXT                 │ NOT NULL                  │
│ tipo_material            │ VARCHAR(30)          │ NOT NULL                  │
│                          │                      │ CHECK IN (                │
│                          │                      │  'ENTULHO',               │
│                          │                      │  'AZULEJO_CERAMICA',      │
│                          │                      │  'MADEIRA',               │
│                          │                      │  'METAL',                 │
│                          │                      │  'OUTRO')                 │
│ quantidade               │ VARCHAR(100)         │ nullable                  │
│                          │                      │ texto livre               │
│                          │                      │ ex: "2 sacos de areia"    │
│ foto_url                 │ VARCHAR(500)         │ nullable (MinIO)          │
│ zona_id                  │ UUID                 │ NOT NULL                  │
│                          │                      │ FK → zonas(id)            │
│                          │                      │ zona do anúncio —         │
│                          │                      │ sem localização exacta    │
│                          │                      │ pública RF-15             │
│ estado                   │ VARCHAR(20)          │ NOT NULL                  │
│                          │                      │ DEFAULT 'DISPONIVEL'      │
│                          │                      │ CHECK IN (                │
│                          │                      │  'DISPONIVEL',            │
│                          │                      │  'RESERVADO',             │
│                          │                      │  'CONCLUIDO',             │
│                          │                      │  'EXPIRADO',              │
│                          │                      │  'REMOVIDO')              │
│ expira_em                │ TIMESTAMPTZ          │ NOT NULL                  │
│                          │                      │ DEFAULT now() + 30 days   │
│ disclaimer_aceite        │ BOOLEAN              │ NOT NULL, DEFAULT false   │
│                          │                      │ RF-15: "disclaimers legais│
│                          │                      │ obrigatórios"             │
│ criado_em                │ TIMESTAMPTZ          │ NOT NULL, DEFAULT now()   │
│ atualizado_em            │ TIMESTAMPTZ          │ NOT NULL, DEFAULT now()   │
└──────────────────────────┴──────────────────────┴───────────────────────────┘

Índices:
  (zona_id, estado, criado_em DESC)  BTREE PARTIAL WHERE estado='DISPONIVEL'
  (cidadao_id, criado_em DESC)       BTREE — "os meus anúncios"
  (expira_em)                        BTREE — job de expiração automática
```