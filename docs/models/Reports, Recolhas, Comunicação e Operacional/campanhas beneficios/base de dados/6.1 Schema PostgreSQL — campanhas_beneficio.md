```
┌─────────────────────────────────────────────────────────────────────────────┐
│  tabela: campanhas_beneficio                                                │
├──────────────────────────┬──────────────────────┬───────────────────────────┤
│ coluna                   │ tipo                 │ constraints / notas       │
├──────────────────────────┼──────────────────────┼───────────────────────────┤
│ id                       │ UUID                 │ PK                        │
│ titulo                   │ VARCHAR(200)         │ NOT NULL                  │
│ descricao                │ TEXT                 │ NOT NULL                  │
│ texto_elegibilidade      │ TEXT                 │ NOT NULL                  │
│                          │                      │ RF-21: "textos claros     │
│                          │                      │ de elegibilidade"         │
│ zona_id                  │ UUID                 │ NOT NULL                  │
│                          │                      │ FK → zonas(id)            │
│ criterio_tipo            │ VARCHAR(50)          │ NOT NULL                  │
│                          │                      │ CHECK IN (                │
│                          │                      │  'QUIZ_PARTICIPACAO',     │
│                          │                      │  'RECOLHA_AGENDADA',      │
│                          │                      │  'REPORTS_COUNT',         │
│                          │                      │  'COMBINADO')             │
│ criterio_config          │ JSONB                │ NOT NULL                  │
│                          │                      │ RF-21: comportamentos     │
│                          │                      │ observáveis não pessoais  │
│                          │                      │ {min_quizzes: 2,          │
│                          │                      │  min_recolhas: 1}         │
│ beneficio_descricao      │ TEXT                 │ NOT NULL                  │
│ ativa                    │ BOOLEAN              │ NOT NULL, DEFAULT false   │
│ inicio_em                │ TIMESTAMPTZ          │ NOT NULL                  │
│ fim_em                   │ TIMESTAMPTZ          │ NOT NULL                  │
│                          │                      │ CHECK fim_em > inicio_em  │
│ criado_por               │ UUID                 │ NOT NULL                  │
│                          │                      │ FK → users(id)            │
│ criado_em                │ TIMESTAMPTZ          │ NOT NULL, DEFAULT now()   │
└──────────────────────────┴──────────────────────┴───────────────────────────┘

Índices:
  (zona_id, ativa, fim_em)    BTREE PARTIAL WHERE ativa=true
  (inicio_em, fim_em)         BTREE — campanhas em vigor num período
```