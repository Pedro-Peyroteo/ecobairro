```
┌─────────────────────────────────────────────────────────────────────────────┐
│  tabela: pedidos_recolha                                                    │
├──────────────────────────┬──────────────────────┬───────────────────────────┤
│ coluna                   │ tipo                 │ constraints / notas       │
├──────────────────────────┼──────────────────────┼───────────────────────────┤
│ id                       │ UUID                 │ PK, gen_random_uuid()     │
│ numero                   │ BIGINT               │ NOT NULL, UNIQUE          │
│                          │                      │ sequence — visível        │
│                          │                      │ ex: REC-2025-000007       │
├──────────────────────────┼──────────────────────┼───────────────────────────┤
│ cidadao_id               │ UUID                 │ NOT NULL                  │
│                          │                      │ FK → users(id)            │
│ zona_id                  │ UUID                 │ NOT NULL                  │
│                          │                      │ FK → zonas(id)            │
├──────────────────────────┼──────────────────────┼───────────────────────────┤
│ morada_recolha           │ TEXT                 │ NOT NULL                  │
│ localizacao_recolha      │ GEOGRAPHY(POINT,4326)│ nullable                  │
│                          │                      │ georeferenciação opcional │
│ tipo_residuo             │ VARCHAR(30)          │ NOT NULL                  │
│                          │                      │ CHECK IN (                │
│                          │                      │  'MONO',                  │
│                          │                      │  'ENTULHO_PEQUENO',       │
│                          │                      │  'ELECTRODOMESTICO',      │
│                          │                      │  'MOBILIARIO',            │
│                          │                      │  'OUTRO')                 │
│ descricao                │ TEXT                 │ NOT NULL                  │
│ volume_estimado          │ VARCHAR(20)          │ nullable                  │
│                          │                      │ 'PEQUENO','MEDIO','GRANDE'│
│ foto_url                 │ VARCHAR(500)         │ nullable (MinIO)          │
├──────────────────────────┼──────────────────────┼───────────────────────────┤
│ estado                   │ VARCHAR(20)          │ NOT NULL                  │
│                          │                      │ DEFAULT 'PENDENTE'        │
│                          │                      │ CHECK IN (                │
│                          │                      │  'PENDENTE',              │
│                          │                      │  'AGENDADO',              │
│                          │                      │  'EM_CURSO',              │
│                          │                      │  'CONCLUIDO',             │
│                          │                      │  'CANCELADO',             │
│                          │                      │  'REJEITADO')             │
│ data_agendada            │ DATE                 │ nullable                  │
│ operador_id              │ UUID                 │ nullable                  │
│                          │                      │ FK → users(id)            │
│                          │                      │ operador responsável      │
│ notas_operador           │ TEXT                 │ nullable                  │
│ motivo_rejeicao          │ TEXT                 │ nullable                  │
├──────────────────────────┼──────────────────────┼───────────────────────────┤
│ criado_em                │ TIMESTAMPTZ          │ NOT NULL, DEFAULT now()   │
│ atualizado_em            │ TIMESTAMPTZ          │ NOT NULL, DEFAULT now()   │
│ eliminado_em             │ TIMESTAMPTZ          │ nullable (24 meses)       │
└──────────────────────────┴──────────────────────┴───────────────────────────┘

Índices:
  (numero)                         UNIQUE BTREE
  (cidadao_id, criado_em DESC)     BTREE — histórico do cidadão
  (zona_id, estado, criado_em)     BTREE — gestão operacional
  (estado, data_agendada)          BTREE PARTIAL WHERE estado = 'AGENDADO'
```