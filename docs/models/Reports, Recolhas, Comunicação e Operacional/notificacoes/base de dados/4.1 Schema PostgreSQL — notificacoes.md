```
┌─────────────────────────────────────────────────────────────────────────────┐
│  tabela: notificacoes                                                       │
├──────────────────────────┬──────────────────────┬───────────────────────────┤
│ coluna                   │ tipo                 │ constraints / notas       │
├──────────────────────────┼──────────────────────┼───────────────────────────┤
│ id                       │ UUID                 │ PK                        │
│ cidadao_id               │ UUID                 │ NOT NULL                  │
│                          │                      │ FK → users(id)            │
│ tipo                     │ VARCHAR(30)          │ NOT NULL                  │
│                          │                      │ CHECK IN (                │
│                          │                      │  'REPORT_ESTADO',         │
│                          │                      │  'ECOPONTO_CHEIO',        │
│                          │                      │  'RECOLHA_AGENDADA',      │
│                          │                      │  'RECOLHA_CONCLUIDA',     │
│                          │                      │  'BADGE_GANHO',           │
│                          │                      │  'QUIZ_DISPONIVEL',       │
│                          │                      │  'MENSAGEM_PARTILHA',     │
│                          │                      │  'AVISO_INSTITUCIONAL')   │
│ titulo                   │ VARCHAR(200)         │ NOT NULL                  │
│ corpo                    │ TEXT                 │ NOT NULL                  │
│ payload                  │ JSONB                │ NOT NULL, DEFAULT '{}'    │
│                          │                      │ {report_id, ecoponto_id,  │
│                          │                      │  badge_id, ...}           │
│                          │                      │ deep link no frontend     │
│ canal                    │ VARCHAR(10)          │ NOT NULL                  │
│                          │                      │ CHECK IN (                │
│                          │                      │  'PUSH','EMAIL','SMS',    │
│                          │                      │  'IN_APP')                │
│ estado_envio             │ VARCHAR(20)          │ NOT NULL                  │
│                          │                      │ DEFAULT 'PENDENTE'        │
│                          │                      │ CHECK IN (                │
│                          │                      │  'PENDENTE',              │
│                          │                      │  'ENVIADO',               │
│                          │                      │  'ENTREGUE',              │
│                          │                      │  'FALHOU')                │
│ lida                     │ BOOLEAN              │ NOT NULL, DEFAULT false   │
│ lida_em                  │ TIMESTAMPTZ          │ nullable                  │
│ enviado_em               │ TIMESTAMPTZ          │ nullable                  │
│ criado_em                │ TIMESTAMPTZ          │ NOT NULL, DEFAULT now()   │
└──────────────────────────┴──────────────────────┴───────────────────────────┘

Índices:
  (cidadao_id, lida, criado_em DESC)       BTREE — inbox não lidas
  (cidadao_id, tipo, criado_em DESC)       BTREE — filtro por tipo
  (estado_envio, criado_em)                BTREE PARTIAL WHERE estado='PENDENTE'
  (criado_em)                              BTREE — archiving 24 meses
```