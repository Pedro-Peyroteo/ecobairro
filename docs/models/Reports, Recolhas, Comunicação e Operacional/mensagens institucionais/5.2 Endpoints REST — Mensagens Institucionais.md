| #   | Método   | Rota                                           | Descrição                                       | Auth                     | Fluxo                                                   |
| --- | -------- | ---------------------------------------------- | ----------------------------------------------- | ------------------------ | ------------------------------------------------------- |
| MI1 | `GET`    | `/mensagens-institucionais`                    | Listar mensagens activas para a zona do cidadão | CIDADAO                  | NestJS → Redis cache 10min ou PG réplica                |
| MI2 | `GET`    | `/mensagens-institucionais/:id`                | Detalhe de mensagem                             | CIDADAO                  | NestJS → PG réplica                                     |
| MI3 | `GET`    | `/admin/mensagens-institucionais`              | Listar todas incluindo rascunhos                | TECNICO_AUTARQUIA, ADMIN | NestJS → PG réplica                                     |
| MI4 | `POST`   | `/admin/mensagens-institucionais`              | Criar mensagem segmentada RF-17                 | TECNICO_AUTARQUIA, ADMIN | NestJS → PG write                                       |
| MI5 | `PUT`    | `/admin/mensagens-institucionais/:id`          | Editar (só se não publicada)                    | TECNICO_AUTARQUIA, ADMIN | NestJS → valida publicada=false → PG write              |
| MI6 | `PATCH`  | `/admin/mensagens-institucionais/:id/publicar` | Publicar e enviar notificações                  | TECNICO_AUTARQUIA, ADMIN | NestJS → PG write publicada=true → BullMQ fan-out notif |
| MI7 | `DELETE` | `/admin/mensagens-institucionais/:id`          | Remover ou expirar mensagem                     | ADMIN                    | NestJS → PG write expira_em=now()                       |

**Cache Redis — Mensagens Institucionais:**

```
┌──────────────────────────────────┬──────────┬───────────────────────────────┐
│ Chave                            │ TTL      │ Invalidação                   │
├──────────────────────────────────┼──────────┼───────────────────────────────┤
│ mensagens:zona:{zona_id}         │ 10 min   │ MI6 publicar → DEL zonas      │
│ (mensagens activas para zona)    │          │ abrangidas                    │
└──────────────────────────────────┴──────────┴───────────────────────────────┘
```