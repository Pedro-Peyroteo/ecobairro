```
┌─────────────────────────────────────────────────────────────────────────────┐
│  tabela: mensagens_institucionais                                           │
├──────────────────────────┬──────────────────────┬───────────────────────────┤
│ coluna                   │ tipo                 │ constraints / notas       │
├──────────────────────────┼──────────────────────┼───────────────────────────┤
│ id                       │ UUID                 │ PK                        │
│ titulo                   │ VARCHAR(200)         │ NOT NULL                  │
│ corpo                    │ TEXT                 │ NOT NULL                  │
│ tipo                     │ VARCHAR(30)          │ NOT NULL                  │
│                          │                      │ CHECK IN (                │
│                          │                      │  'ALTERACAO_ROTA',        │
│                          │                      │  'CAMPANHA',              │
│                          │                      │  'AVISO_OPERACIONAL',     │
│                          │                      │  'INFORMACAO_GERAL')      │
│ zonas_destino            │ UUID[]               │ NOT NULL, DEFAULT '{}'    │
│                          │                      │ vazio = todas as zonas    │
│ publicada                │ BOOLEAN              │ NOT NULL, DEFAULT false    │
│ publicada_em             │ TIMESTAMPTZ          │ nullable                  │
│ expira_em                │ TIMESTAMPTZ          │ nullable                  │
│ criado_por               │ UUID                 │ NOT NULL                  │
│                          │                      │ FK → users(id)            │
│ criado_em                │ TIMESTAMPTZ          │ NOT NULL, DEFAULT now()   │
└──────────────────────────┴──────────────────────┴───────────────────────────┘

Índices:
  (publicada, expira_em)               BTREE PARTIAL WHERE publicada=true
  (zonas_destino)                      GIN — query zonas_destino @> ARRAY[zona_id]
  (criado_em DESC)                     BTREE — listagem admin
```

---